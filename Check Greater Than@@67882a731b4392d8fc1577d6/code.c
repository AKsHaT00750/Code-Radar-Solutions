#include <stdio.h>

int  main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if (a>b) {
        return printf("True");
    }
    else {
        return printf("False");
    }
    return 0;
    
}

